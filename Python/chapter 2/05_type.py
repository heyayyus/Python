#  type function is used to get the type of the variable    type(variable)  
#  we can also change the type of the variable using the type casting functions like int(), float(), str() etc. 
#  int() is used to convert a variable to integer
#  float() is used to convert a variable to float   
#  str() is used to convert a variable to string
#  bool() is used to convert a variable to boolean
#  None is used to represent the absence of a value or a null value.


a = "31.2"
b = float(a) # a but the type should be float
t = type(b) 

print(t)